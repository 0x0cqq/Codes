#include <cstdio>
#include <string>
#include <vector>
using namespace std;

int n;

struct bint{
    vector<int> x;
    bint operator +(const bint a)const{
        bint b;
        for(int i = 0;i<)
    }
};

int main(){
    scanf("%d",&n);
    for(int i = 1;i<=n;i++){
        for(int j = 0;last[j];j++)
    }
    return 0;
}